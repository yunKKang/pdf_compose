# PDF Compose

论文图表拼版 Web 工具（Vue 3 + Fabric.js + PDF.js + pdf-lib）。

## 功能概览

- 多 PDF 连续导入，统一选页后拼版
- PDF 页面拖拽到画布，支持裁剪、对齐、图层、模板、撤销重做
- SVG 资产导入并参与拼版
- 导出 PDF：PDF 对象尽量保矢量，SVG 走矢量优先 + 位图降级
- 导出前对象预检，减少“导出后才发现失败”

## 本地运行

```bash
pnpm install
pnpm dev
```

生产构建与回归：

```bash
pnpm build
pnpm regression
```

## 如何用本地 PDF 测试

1. 打开页面后，左侧点击“选择 PDF”上传本地论文 PDF。
2. 在左侧缩略图区单击页面选择：
   - `Ctrl/Cmd + 单击` 多选
   - `Shift + 单击` 连续选择
3. 点击“加入画布”或直接拖拽缩略图到中间画布。
4. 在画布中排版：可拖动、缩放、裁剪，必要时设置“导出页”并点击“分配到页”。
5. 点击顶部“导出 PDF”，浏览器会下载拼版结果。

## 画布区 vs 下方说明区

- 画布区：真正参与导出的对象编辑区。
- 下方说明区：仅教程说明，不参与导出。
- 左侧缩略图：仅选页与入画布入口，不会直接导出。

## 选择对象与微调

- 先在画布中点击对象，再使用上方操作按钮（裁剪、对齐、图层、分配页等）。
- 方向键：每次移动 `1px`。
- `Shift + 方向键`：每次移动 `10px`。
- 开启吸附时，拖动会显示红色对齐线。

## 网格线说明

- 网格用于辅助定位与间距判断。
- 网格显示在画布上层，不会被对象完全遮挡。
- 可通过“隐藏网格/显示网格”切换可见性。
- 吸附开关控制是否按网格步进吸附。

## 导出失败排查

### 1) `No PDF header found`

说明某个被当作 PDF 读取的输入不是合法 PDF 字节流。

排查顺序：

1. 重新从左侧上传源 PDF（不要只改扩展名）。
2. 删除画布中对应对象，从左侧重新加入画布。
3. 若文件来源于截图/图片转存，请先用可靠工具导出为标准 PDF 后再导入。

### 2) 提示“存在无法导出的对象”

- 画布里有对象元数据缺失或来源文件已失效。
- 删除该对象并从左侧重新拖入/加入即可。

### 3) 画布里拼好但导出结果不完整

- 检查是否给对象分配到了错误导出页。
- 导出是按“导出页”分组输出，未分配时默认在第 1 页。

## 真实论文样本精度复核

- 将真实样本放到 `samples/real/`。
- 按 `samples/real/precision_review_report_template.md` 逐项记录位置、尺寸、清晰度与导出结果。
- 建议每次改动导出链路后复跑一次并留存报告。
